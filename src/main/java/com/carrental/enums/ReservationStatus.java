package com.carrental.enums;

public enum ReservationStatus {
    CONFIRMED,   // Active reservation — car is held
    CANCELLED,   // Canceled by the customer
    COMPLETED    // Rental period has ended
}
